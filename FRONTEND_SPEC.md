# ShymOpt — Техническое задание на фронтенд

## Стек

- **React 18** + **Vite 5**
- **TypeScript**
- **Tailwind CSS** — стилизация
- **convex** — клиент для Convex API
- **react-router-dom** — маршрутизация
- **lucide-react** — иконки

## Структура проекта

```
src/
├── main.tsx
├── App.tsx
├── index.css
├── convex/                 # Convex клиент
│   └── _generated/
├── components/
│   ├── ui/                 # Базовые UI компоненты
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   ├── Modal.tsx
│   │   ├── Badge.tsx
│   │   ├── Rating.tsx
│   │   └── Loading.tsx
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   ├── Sidebar.tsx
│   │   └── Layout.tsx
│   ├── lots/
│   │   ├── LotCard.tsx
│   │   ├── LotGrid.tsx
│   │   ├── LotDetail.tsx
│   │   └── LotFilters.tsx
│   ├── farmers/
│   │   ├── FarmerCard.tsx
│   │   ├── FarmerProfile.tsx
│   │   └── FarmerReviews.tsx
│   ├── cart/
│   │   ├── CartItem.tsx
│   │   ├── CartSummary.tsx
│   │   └── CartDrawer.tsx
│   ├── orders/
│   │   ├── OrderCard.tsx
│   │   ├── OrderList.tsx
│   │   ├── OrderDetail.tsx
│   │   └── OrderStatus.tsx
│   └── reviews/
│       ├── ReviewCard.tsx
│       ├── ReviewForm.tsx
│       └── RatingStars.tsx
├── pages/
│   ├── Home.tsx
│   ├── Catalog.tsx
│   ├── LotDetailPage.tsx
│   ├── FarmersList.tsx
│   ├── FarmerProfilePage.tsx
│   ├── Cart.tsx
│   ├── Checkout.tsx
│   ├── Orders.tsx
│   ├── OrderDetailPage.tsx
│   ├── Profile.tsx
│   ├── Login.tsx
│   ├── Register.tsx
│   └── farmer/
│       ├── Dashboard.tsx
│       ├── MyLots.tsx
│       ├── MyOrders.tsx
│       └── MyReviews.tsx
├── hooks/
│   ├── useAuth.ts
│   ├── useCart.ts
│   └── useConvex.ts
├── lib/
│   ├── convex.ts
│   └── utils.ts
└── types/
    └── index.ts
```

---

## Страницы и функционал

### 1. Главная (`/`)

- Hero-секция: "ShymOpt — Оптовые закупки у фермеров"
- Как это работает (3 шага для покупателя, 3 для фермера)
- Топ фермеров (карточки с рейтингом)
- Популярные категории
- CTA: "Стать фермером" / "Начать закупки"

### 2. Каталог (`/catalog`)

**Фильтры:**
- Категория (выпадающий список с иконками)
- Цена (диапазон)
- Фермер
- Поиск по названию

**Карточки лотов:**
- Фото товара
- Название + категория
- Цена за единицу тары + вес тары
- Фермер + рейтинг
- Кнопка "Добавить в корзину"

### 3. Детали лота (`/catalog/:id`)

- Фото товара (галерея)
- Название, описание
- Цена за единицу тары
- Вес тары
- Тип тары (ящик/коробка/мешок)
- Минимальный заказ
- Доступное количество
- Срок годности
- Инструкции по хранению

**Секция фермера:**
- Имя фермера
- Адрес
- Рейтинг (звезды)
- Количество отзывов
- Ссылка на профиль фермера

**Секция отзывов:**
- Список отзывов с оценками
- Средний рейтинг

**Кнопки:**
- "Добавить в корзину" (с выбором количества)
- "Перейти к фермеру"

### 4. Список фермеров (`/farmers`)

- Карточки фермеров:
  - Имя
  - Адрес
  - Рейтинг
  - Количество лотов
  - Количество отзывов

### 5. Профиль фермера (`/farmers/:id`)

- Имя, адрес, телефон
- Рейтинг + распределение оценок (5★, 4★...)
- Описание (опционально)

**Лоты фермера:**
- Сетка карточек лотов

**Отзывы:**
- Список отзывов с оценками и текстом

### 6. Корзина (`/cart`)

- Список товаров:
  - Фото + название
  - Тип тары + вес
  - Цена × Количество
  - Кнопки +/- для количества
  - Кнопка удаления

- Итого:
  - Количество единиц
  - Общая сумма

- Кнопка "Оформить заказ"

### 7. Оформление заказа (`/checkout`)

- Способ оплаты: Наличные / Перевод
- Время самовывоза (выбор даты и времени)
- Заметки к заказу
- Адрес самовывоза (показывается адрес фермера)
- Итоговая сумма
- Кнопка "Подтвердить заказ"

### 8. Мои заказы (`/orders`)

- Список заказов с фильтрацией по статусу
- Карточка заказа:
  - Номер + дата
  - Фермер
  - Товары (список)
  - Сумма
  - Статус (цветной бейдж)
  - Кнопка "Детали"

### 9. Детали заказа (`/orders/:id`)

- Статус (progress bar: pending → confirmed → ready → completed)
- Информация о фермере (имя, телефон, адрес)
- Список товаров
- Способ оплаты
- Время самовывоза
- Заметки

**Для покупателя:**
- Кнопка "Оставить отзыв" (если статус = completed)

**Для фермера:**
- Кнопки смены статуса (подтвердить → готов → завершён)

### 10. Профиль (`/profile`)

- Имя, почта, телефон
- Роль (фермер/покупатель)
- Кнопка редактирования

**Для покупателя:**
- Статистика: заказов, потрачено
- Мои отзывы

**Для фермера:**
- Статистика: заказов, выручка
- Ссылка на фермерский дашборд

### 11. Фермерский дашборд (`/farmer/dashboard`)

**Статистика:**
- Заказов сегодня / за неделю / за месяц
- Выручка за период
- Активных лотов
- Средний рейтинг

**Последние заказы:**
- Топ-5 заказов со статусами

**Быстрые действия:**
- Создать лот
- Посмотреть все заказы

### 12. Мои лоты (`/farmer/lots`)

- Сетка лотов фермера
- Статус каждого (active/draft/sold_out)
- Кнопки: редактировать, деактивировать

### 13. Заказы фермера (`/farmer/orders`)

- Фильтр по статусу
- Карточки заказов:
  - Покупатель (имя, телефон)
  - Товары
  - Сумма
  - Статус
  - Кнопки смены статуса

### 14. Отзывы фермера (`/farmer/reviews`)

- Средний рейтинг + распределение
- Список отзывов

---

## Авторизация (Demo)

Простая авторизация без пароля:

```
/login:
  - Ввод email
  - Кнопка "Войти"
  - Если email есть в базе → вход
  - Если нет → редирект на /register

/register:
  - Имя
  - Email
  - Телефон
  - Роль: фермер / покупатель
  - (Для фермера) Адрес
  - Кнопка "Зарегистрироваться"
```

Хранение: `localStorage` с `userId`.

---

## Компоненты UI

### LotCard
```
┌──────────────────────┐
│      [Фото]          │
│                      │
│  🥕 Морковь          │
│  Овощи               │
│                      │
│  500₸ / ящик         │
│  Ящик 10 кг          │
│                      │
│  ⭐ 4.8 (23 отзыва)  │
│  🏠 Ферма "Дачная"   │
│                      │
│  [Добавить в корзину]│
└──────────────────────┘
```

### OrderStatus (Progress)
```
  pending → confirmed → ready → completed
    ⏳         ✅         📦        ✅
    │          │          │         │
    └──────────┴──────────┴─────────┘
         Текущий статус: ready
```

### CartDrawer (Выдвижная панель)
```
┌─────────────────────┐
│ Корзина (3 товара)  │
│─────────────────────│
│ 🥕 Морковь          │
│ Ящик 10кг × 2       │
│ 1000₸              │
│                     │
│ 🥔 Картошка         │
│ Мешок 5кг × 1       │
│ 300₸               │
│─────────────────────│
│ Итого: 1300₸       │
│ [Оформить заказ]    │
└─────────────────────┘
```

---

## Стили

**Цвета:**
- Primary: `#22c55e` (зелёный — фермерская тематика)
- Secondary: `#f97316` (оранжевый — акцент)
- Background: `#fafaf9` (светло-серый)
- Text: `#1c1917`

**Шрифт:** Inter / Nunito

---

## Конвекс клиент

```typescript
// lib/convex.ts
import { ConvexProvider, ConvexReactClient } from "convex/react";

const convex = new ConvexReactClient(import.meta.env.VITE_CONVEX_URL);

export { convex };
```

---

## Запуск

```bash
npm create vite@latest shym-frontend -- --template react-ts
cd shym-frontend
npm install
npm install convex react-router-dom lucide-react tailwindcss @tailwindcss/vite
npm run dev
```

VITE_CONVEX_URL берётся из `.env.local` (автоматически при `npx convex dev`).
